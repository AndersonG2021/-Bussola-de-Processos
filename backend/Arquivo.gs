/**
 * Arquivo.gs — acesso ao Google Drive para armazenamento de
 * documentos dos processos (Termos Aditivos, Termos de Compromisso,
 * Restituição de Déficit Financeiro).
 *
 * Responsabilidade futura: salvar uploads recebidos do frontend em
 * pastas do Drive organizadas por processo, e devolver links/ids
 * para gravar na planilha.
 *
 * STUB: nenhuma lógica implementada ainda.
 */
